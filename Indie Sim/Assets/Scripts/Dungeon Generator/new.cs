/*
using System;
using System.Collections.Generic;
using UnityEngine;
using System.Linq;
using UnityEngine.Tilemaps;

public enum RoomType
{
    start_room,

    end_room,

    mian_artery_room,

    leaf_node_room,
    
}

public enum ConnectionType
{
    artery_path,
    vein_path,
}

[System.Serializable]
public struct RoomConnection
{
    public int connected_room_ID;
    public ConnectionType type;

    public Vector2 connection_point;

    public RoomConnection(int roomID, ConnectionType connection_type, Vector2 conn_point)
    {
        connected_room_ID = roomID;
        type = connection_type;
        connection_point = conn_point;
    }
}

*/
